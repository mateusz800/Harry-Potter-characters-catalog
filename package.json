{
  "name": "character-catalog",
  "version": "1.0.0",
  "description": "\"Harry Potter\" character catalog",
  "main": "./js/app.js",
  "scripts": {
    "start": "http-server --port 8080 --proxy 'http://localhost:8080?'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mateusz Budnik",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1",
    "serve": "^14.0.1"
  }
}
